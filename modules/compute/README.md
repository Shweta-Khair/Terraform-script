# compute

Security groups (bastion SSH from CIDR; private instance SSH only from bastion) and two EC2 instances on Amazon Linux 2023.

**Inputs / outputs:** `variables.tf`, `outputs.tf`. Consumed by the root stack as `module.compute`.
