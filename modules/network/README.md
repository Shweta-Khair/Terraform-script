# network

VPC, one public and one private subnet in a single AZ, Internet Gateway, NAT Gateway, and route tables.

**Inputs / outputs:** `variables.tf`, `outputs.tf`. Consumed by the root stack as `module.network`.
