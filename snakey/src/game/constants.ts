/** Grid cells wide/tall (MVP; tune later). */
export const GRID_SIZE = 20;

/** World units per grid cell. */
export const CELL_SIZE = 1;

/** Fixed timestep for game logic (seconds). */
export const TICK_SECONDS = 0.12;
